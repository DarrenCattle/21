<section class="feed">
	<div class="container">
		<div class="col-md-8 col-md-offset-2 feed-container">
			<ul>
			<% @activities.each do |activity| %>
				<li class="activity">
					<%= link_to activity.owner.name, user_path(activity.owner) if activity.owner %>
					<%= render_activity activity %>
				</li>
			<% end %>
			</ul>
		</div> <!-- end col -->
	</div> <!-- end container -->
</section>