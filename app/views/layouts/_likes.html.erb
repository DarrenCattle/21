<% if activity.likes.count > 0 %>
	<div class="likes-container">
		<ul class="likes">
			<% activity.likes.each do |like| %>
				<li class="like" data-toggle="tooltip" data-placement="top" title="Tooltip on top"><%= image_tag like.user.gravatar_url, class: "img-circle" %></li>
			<% end %>
			like this.
		</ul>
	</div>
<% end %>