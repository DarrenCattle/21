forfeited their challenge: 

<% if activity.trackable %>
  <li class="activity destroy">
		<div class="row">
			<div class="profile-pic-container">
				<%= image_tag activity.owner.gravatar_url, class: "img-circle center-block img-responsive" %>
			</div>
			<div class="content">
				<%= link_to activity.owner.name, user_path(activity.owner), class: "name" if activity.owner %> forfeited his challenge. 
				<span class="time-ago"><%= time_ago_in_words(activity.created_at) %> ago.</span><br>
				<%= link_to activity.trackable.name, activity.trackable, class: "challenge" if activity.owner %><br>
				<%= activity.message %>
			</div>
		</div>
	</li>
<% else %>
  which has since been removed
<% end %>