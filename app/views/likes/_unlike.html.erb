<%= form_for(current_user.likes.find_by(activity_id: activity.id), :html => { :method => :delete }, :remote => true) do |f| %>
	<div><%= f.hidden_field :activity_id, value: activity.id %></div>
	<div class="actions"><%= f.submit "Unlike", class: "balloon" %></div>
<% end %>